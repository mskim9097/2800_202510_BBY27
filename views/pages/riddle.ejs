<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Biodiversity Riddles</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Camera icon -->
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        #content {
            -ms-overflow-style: none;
            scrollbar-width: none;
            overflow-y: scroll;
        }

        #content::-webkit-scrollbar,body::-webkit-scrollbar {
            display: none;
        }
    </style>
</head>

<body class="h-screen overflow-y-auto">
    <div style="background-color:rgb(242, 242, 242)"
        class="flex flex-col p-4 justify-between h-full rounded-lg shadow-sm gap-5">
        <div id="content" class="flex flex-col gap-4 overflow-y-auto">
            <%- include("../partials/header") %>

            <div class="flex items-center gap-5 mt-4">
                <img class="w-10 h-10" src="/images/photo.svg" alt="Profile Icon">
                <div>
                    <span class="text-xl">Hello,</span> <span class="font-bold text-xl">
                        <%= name %>
                    </span>
                </div>
            </div>

            <!-- Riddle Form -->
            <div class="max-w-xl mx-auto bg-white p-6 rounded-xl shadow-md">
                <h1 class="text-2xl font-bold text-center mb-6">🧠 Biodiversity Riddle Challenge</h1>
                <form action="/ai/riddles" method="POST" class="space-y-6">
                    <div>
                        <label class="block text-sm font-medium text-gray-700">Difficulty Level</label>
                        <select name="difficulty" class="mt-2 block w-full border border-gray-300 rounded-md p-2 bg-white">
                            <option>easy</option>
                            <option>medium</option>
                            <option>hard</option>
                        </select>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700">Number of Riddles</label>
                        <input type="number" name="number" value="5" min="1" max="10"
                            class="mt-2 block w-full border border-gray-300 rounded-md p-2" />
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700">Category</label>
                        <select name="category" class="mt-2 block w-full border border-gray-300 rounded-md p-2 bg-white">
                            <option value="any">Any</option>
                            <option value="birds">Birds</option>
                            <option value="plants">Plants</option>
                            <option value="insects">Insects</option>
                        </select>
                    </div>

                    <div class="text-center pt-4">
                        <button type="submit"
                            class="bg-green-600 text-white px-6 py-3 rounded-lg hover:bg-green-700 transition duration-200 font-semibold">
                            🪄 Generate Riddles
                        </button>
                    </div>
                </form>
            </div>
        </div>
        
        <%- include("../partials/explorer-footer") %>
    </div>

    <script src="/js/menu.js"></script>
    <script src="/js/camera.js"></script>
</body>

</html>