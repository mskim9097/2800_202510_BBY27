<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Page Not Found - BioDex</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body style="background-image: url('/images/logo-3.png'); background-size: 100% 100%; background-repeat: no-repeat;" class="min-h-screen flex flex-col items-center justify-center p-8">
    <div class="w-full max-w-2xl mx-auto text-center">
        <!-- Content -->
        <div class="bg-white/90 backdrop-blur-sm p-8 rounded-2xl shadow-lg">
            <h1 class="text-4xl font-bold text-gray-800 mb-4">404 - Page Not Found</h1>
            <p class="text-xl text-gray-600 mb-8">
                Sorry, the page you are looking for does not exist or the species could not be found.
            </p>

            <!-- Conditional Navigation Button -->
            <% if (locals.isAuthenticated) { %>
                <% if (locals.userType === 'researcher') { %>
                    <a href="/user/researcher" 
                        class="inline-flex items-center px-6 py-3 bg-green-600 text-white font-semibold rounded-lg hover:bg-green-700 transition duration-200">
                        <span class="mr-2">🏠</span>
                        Return to Dashboard
                    </a>
                <% } else { %>
                    <a href="/user/explorer" 
                        class="inline-flex items-center px-6 py-3 bg-green-600 text-white font-semibold rounded-lg hover:bg-green-700 transition duration-200">
                        <span class="mr-2">🏠</span>
                        Return to Dashboard
                    </a>
                <% } %>
            <% } else { %>
                <a href="/" 
                    class="inline-flex items-center px-6 py-3 bg-green-600 text-white font-semibold rounded-lg hover:bg-green-700 transition duration-200">
                    <span class="mr-2">🌍</span>
                    Return to Homepage
                </a>
            <% } %>
        </div>
    </div>
</body>
</html> 