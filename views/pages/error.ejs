<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - BiodiversityGo</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-no-repeat bg-center bg-fixed bg-cover min-h-screen flex items-center justify-center"
    style="background-image: url('/images/plant2.jpg');">

    <div class="bg-white/10 backdrop-blur-sm p-8 rounded-xl shadow-xl max-w-md w-full">
        <div class="text-center">
            <div class="text-red-500 mb-4">
                <svg class="mx-auto h-16 w-16" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                          d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
                </svg>
            </div>
            
            <h1 class="text-2xl font-bold text-white mb-4">Oops! Something went wrong</h1>
            
            <p class="text-gray-200 mb-8"><%= error %></p>

            <div class="space-y-4">
                <% if (typeof userType !== 'undefined' && userType) { %>
                    <% if (userType === 'researcher') { %>
                        <a href="/user/researcher" 
                           class="block w-full bg-green-600 text-white px-6 py-3 rounded-lg hover:bg-green-700 transition duration-200">
                            Return to Researcher Dashboard
                        </a>
                    <% } else if (userType === 'explorer') { %>
                        <a href="/user/explorer" 
                           class="block w-full bg-green-600 text-white px-6 py-3 rounded-lg hover:bg-green-700 transition duration-200">
                            Return to Explorer Dashboard
                        </a>
                    <% } %>
                <% } else { %>
                    <a href="/login" 
                       class="block w-full bg-green-600 text-white px-6 py-3 rounded-lg hover:bg-green-700 transition duration-200">
                        Log In
                    </a>
                <% } %>

                <a href="/" 
                   class="block w-full bg-gray-600 text-white px-6 py-3 rounded-lg hover:bg-gray-700 transition duration-200">
                    Return to Homepage
                </a>
            </div>
        </div>
    </div>

</body>

</html> 